/**
 * A type used to describe an author of a NodePolus plugin.
 */
export type PluginAuthor = {
  /**
   * The author's name.
   */
  name: string;
  /**
   * The author's email address.
   */
  email?: string;
  /**
   * The author's website.
   */
  website?: string;
};
